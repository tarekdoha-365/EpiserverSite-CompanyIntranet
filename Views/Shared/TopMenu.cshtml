@model  List<EpiserverSite_CompanyIntranet.Entity.MenuItem>

<ul>
    @foreach (var item in @Model)
    {
    <li>
        <a href="@item.Url">@item.Title</a>
        @{Html.RenderPartial("TopMenu", item.MenuItemList);}
    </li>
    }
</ul>